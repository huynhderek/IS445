//Complete the following functions
//Displays all nodes
const bodyNodes = () => {
    var nodes = document.body.childNodes;
    nodes.forEach((node) => {
        console.log(node);
    
});
}

bodyNodes();


